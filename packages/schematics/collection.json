{
  "$schema": "../../node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "ng-add": {
      "factory": "./dist/ng-add/index",
      "schema": "./dist/ng-add/schema.json",
      "description": "Generates Angular PWA icon assets (any + maskable manifest icons, favicon, apple-touch-icon) from a source SVG, and merges them into manifest.webmanifest, ngsw-config.json, and index.html."
    }
  }
}
